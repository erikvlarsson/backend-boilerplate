let mongoose;_522‍.x([["default",()=>_522‍.o]]);_522‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

//for hiding connection string
require("dotenv/config");

//connect to db
const connectToMongoDB = async () => {
  try {
    await mongoose.connect(
      process.env.DATABASE_URL,
      {
        useUnifiedTopology: true,
        useNewUrlParser: true,
      },
      () => {
        console.log("Connected to MongoDB Atlas!");
      }
    );
  } catch (error) {
    console.log("Error while trying connect to MongoDB!");
  }
};

const startServer = (app) => {
  const port = process.env.PORT;
  app.listen(port, () => {
    console.log(`Server is running on port ${port}`);
  });
};

_522‍.d({ connectToMongoDB, startServer });
