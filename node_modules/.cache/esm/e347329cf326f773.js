let express,cors,helmet,bodyParser,Config,Middleware,Routing;_f32‍.w("express",[["default",["express"],function(v){express=v}]]);_f32‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_f32‍.w("helmet",[["default",["helmet"],function(v){helmet=v}]]);_f32‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_f32‍.w("./configuration/Config",[["default",["Config"],function(v){Config=v}]]);_f32‍.w("./middleware/Middleware",[["default",["Middleware"],function(v){Middleware=v}]]);_f32‍.w("./routes/Routing",[["default",["Routing"],function(v){Routing=v}]]);







const app = express();

// app.use(morgan("common"));
app.use(helmet());
app.use(bodyParser.urlencoded({ extended: true }));
app.use(bodyParser.json());

// app.use(
//   cors({
//     origin: ["http://localhost:3000", "http://127.0.0.1:3000"],
//     credentials: true,
//   })
// );

app.use(function (req, res, next) {
  _f32‍.g.console.log(req.method, req.url);
  res.header("Access-Control-Allow-Origin", "*");
  res.header(
    "Access-Control-Allow-Headers",
    "Origin, X-Requested-With, Content-Type, Accept, x-token"
  );
  if (req.method === "OPTIONS") {
    res.end();
  } else {
    next();
  }
});

// Import Routes
Routing.setRoutes(app);

// Apply Middleware
app.use(Middleware.notFound);
app.use(Middleware.errorHandler);

Config.startServer(app);
Config.connectToMongoDB();
