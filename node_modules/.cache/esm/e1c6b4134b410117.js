let mongoose;_4b4‍.x([["default",()=>_4b4‍.o]]);_4b4‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
const MongoClient = require("mongodb").MongoClient;

//for hiding connection string
require("dotenv/config");

//connect to db
const connectToMongoDB = async () => {
  try {
    await mongoose.connect(process.env.DATABASE_URL, {
      keepAlive: true,
      useUnifiedTopology: true,
      useNewUrlParser: true,
    });
    console.log("Connected to MongoDB Atlas!");
  } catch (error) {
    console.log("Error in MongoDB connetion!");
    _4b4‍.g.console.log(error);
  }
};

const startServer = (app) => {
  const port = process.env.PORT;
  app.listen(port, () => {
    console.log(`Server is running on port ${port}`);
  });
};

_4b4‍.d({ connectToMongoDB, startServer });
