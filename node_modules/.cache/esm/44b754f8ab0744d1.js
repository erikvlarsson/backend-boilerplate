let authController;_ab0‍.x([["default",()=>_ab0‍.o]]);_ab0‍.w("../controllers/AuthController.js",[["default",["authController"],function(v){authController=v}]]);

const setRoutes = (app) => {
  app.post("/getRefreshToken", withToken, authController.getRefreshToken);
  app.post("/getAccessToken", withToken, authController.getAccessToken);
  app.post("/login", authController.login);
  app.post("/signup", authController.signup);
  app.get("/clear-all-users", authController.clearUsers);
};

_ab0‍.d({ setRoutes });

const withToken = (req, res, next) => {
  const bearerHeader = req.headers["authorization"];
  _ab0‍.g.console.log(typeof bearerHeader);
  if (typeof bearerHeader !== "undefined") {
    const token = bearerHeader.split(" ")[1];
    req.token = token;
    next();
  } else {
    res.status(401).send("No token found.");
  }
};
